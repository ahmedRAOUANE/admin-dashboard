import React from 'react'

const Footer = ({className}: {className?: string}) => {
    return (
        <footer className={`text-center mt-5 ${className}`}>Copyright © 2024</footer>
    )
}

export default Footer